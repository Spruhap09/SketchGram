import Layout from "@/components/Layout";
import { Typography } from "@material-tailwind/react";
export default function Error404() {
    return <Layout>
        <Typography variant="h1">Error 404: Resource not found</Typography>
    </Layout>
}